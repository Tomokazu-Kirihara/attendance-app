.header
  %h1 Childendance
  %p 出欠確認アプリケーション

.main
  .main__side-ber
    .main__side-ber__days
      = link_to '出欠確認ページへ','#' 
      - current_user.days.each do |day|
        =day.dayname

  .main__main-ber
    .main__main-ber__table-box
      = form_for @day do |f|
        = f.date_field :dayname
        = f.submit "確認", class: "action-btn"
        %p ※日付選択し、出欠チェック後に確認ボタンをクリック
        .main__main-ber__table-box__check
          %table
            %tr
              %th 名前
            = f.collection_check_boxes :user_ids, User.where.not(name: "master"), :id, :name do |user|
              =user.label do
                %tr
                  %td= user.object.name
                  %td= user.check_box


    .main__main-ber__message-form
      .main__main-ber__message-form__user-box
        【お子様がお休みの場合はこちら】
        = link_to edit_user_path(current_user) do
          = icon('fas','cog')
        ユーザー情報編集
      .main__main-ber__message-form__day-set
        = form_for @day do |f|
          = f.date_field :dayname
          = f.submit "確認", class: "action-btn"
      .main__main-ber__message-form__message-set
        = form_for '#' do |f|
          = f.select :absence, [["",0], ["欠席",1], ["遅刻",2], ["早退",3]]
          = f.text_field :comment, class: 'input-message', placeholder: '(例)発熱：38.5℃  インフルエンザ検査の為、病院へ'
          =f.submit '送信', class: 'send-btn'
      .main__main-ber__message-form__session
        ※①日付選択し確認ボタンをクリック  ②欠席or遅刻or早退を選択しその事由を入力後、送信ボタンをクリック